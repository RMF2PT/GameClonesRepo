{
  "name": "game-clones-repo",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --dom --port 4000",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest --ui --coverage.enabled=true"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^0.34.6",
    "@vitest/ui": "^0.34.6",
    "vite": "^4.4.5",
    "vitest": "^0.34.6"
  },
  "dependencies": {
    "happy-dom": "^12.2.2"
  }
}
